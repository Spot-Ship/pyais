version: 1.0
runtime: python3
build:
  commands:
    build:
      - pip install "fastapi[all]"
      - pip install -r requirements.txt
run:
  command: uvicorn main:app
  env:
    - name: DEBUGGING
      value: True
  network:
    port: 8000
